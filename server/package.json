{
  "name": "api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "tsc",
    "start": "NODE_ENV=production node dist/server.js",
    "dev": "NODE_ENV=development ts-node-dev --respawn --transpile-only src/server.ts",
    "debug": "npm run dev",
    "test": "NODE_ENV=development jest --setupFilesAfterEnv=./jest.setup.ts --runInBand",
    "test-env": "jest --setupFilesAfterEnv=./jest.setup.ts --runInBand",
    "test:watch": "NODE_ENV=development jest --watch --setupFilesAfterEnv=./jest.setup.ts --runInBand",
    "test:coverage": "NODE_ENV=development jest --coverage --setupFilesAfterEnv=./jest.setup.ts --runInBand",
    "test-old": "flyway -configFiles=../flyway/flyway.conf -locations=filesystem:../flyway -target=6 clean migrate ; NODE_ENV=development jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^5.1.0",
    "node-flyway": "^0.0.13",
    "pg-promise": "^11.14.0",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/dotenv": "^6.1.1",
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.7",
    "@types/jest": "^29.5.12",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "pg-mem": "^3.0.2",
    "supertest": "^7.0.0",
    "ts-jest": "^29.1.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
